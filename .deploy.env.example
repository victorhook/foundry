# Copy to .deploy.env (gitignored) and fill in your VPS details.
DEPLOY_HOST=your.vps.hostname     # or IP
DEPLOY_USER=deploy                # SSH user (needs sudo to restart the service)
DEPLOY_PATH=/opt/logbook          # where the app lives on the VPS
DEPLOY_SERVICE=logbook            # systemd unit name (default: logbook)
