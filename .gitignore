# Dependencies
node_modules/

# OS
.DS_Store
Thumbs.db

# Local secrets / deploy config (never commit)
.env
.env.*
!.env.example
# Long-lived Claude subscription token from `claude setup-token` (AI chat auth).
# It belongs in /opt/foundry/.env on the server, never in the repo.
.claude_token
*.claude_token
claude_token*
.deploy.env
foundry-deploy
foundry-deploy.pub

# Editors
.idea/

# Saved Claude Code session transcripts (local only)
.session-archive/

# Locally-synced server photos
/photo-backup/

# Local production-build test scratch (server + tesseract cache)
app/.prod*
app/.e2e-data
app/.shotdb/

# AI chat agent scratch workspace (server-side working dir; never committed)
app/ai-workspace/
